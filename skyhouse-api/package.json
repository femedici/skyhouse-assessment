{
  "name": "skyhouse-api",
  "version": "1.0.0",
  "description": "SkyHouse campaign performance API — ingests campaign CSV and serves computed metrics (ROAS, CPA).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.6.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "express": "^4.19.2"
  }
}
